# wanderlust-db-pvc.yaml  (your existing file + storageClassName)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wanderlust-db-pvc
  namespace: wanderlust
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: ""               # disables dynamic provisioning
  resources:
    requests:
      storage: 1Gi
